---
description: P2E plugin conventions — p2e-mode is the sole entry point; MCP is authoritative for domain state. Loads on every chat in this repo so Cursor matches Claude/Codex behavior.
alwaysApply: true
---

# P2E plugin — Cursor policy

This repo ships P2E guidance to Claude Code, Codex, and Cursor through a **single skill**: `p2e-mode`. Legacy `/p2e-*` slash commands and granular workflow skills are removed in v0.12+.

## Hard rules

- **Read `p2e-mode` at session start** (or invoke `/p2e-mode` / run it as a Cursor Custom Mode). MCP surface, lifecycle contract, and assessment gates live in `skills/p2e-mode/SKILL.md` (Codex) and `.cursor/skills/p2e-mode/SKILL.md` (Cursor). Entity/assessment facts live in `references/p2e-model.md` under the same skill.
- **MCP is authoritative.** P2E reads and writes go through `mcp__p2e__*` tools. Do not invent a parallel REST or local-file path.
- **Bind once via `.p2e/project.json`.** If the file is present, honor the bound `product_slug` (legacy `project_slug` still accepted) for every MCP call. If absent, ask the user to create a binding before project-scoped operations.
- **Stories ≠ Issues.** Layers track capability change in P2E; bugs that do not redefine capabilities live in GitHub Issues. No default GH↔story sync — see `references/p2e-model.md` § Stories vs Issues.

## P2E Flow/Foundation model (Patton v3)

A project is a **Product** (`mcp__p2e__products`, `product_slug`). Every Product has two seeded **Flows** — a **persona Flow** (user-journey lane) and an immutable **Foundation Flow** (8 fixed slots). Story → UXO → Phase → Flow → Product. **Wave** is a first-class unbounded package per Product+Release (`W{n}`, no W25 ceiling); call `waves.get` before BUILD membership lists.

## Where to read

- `skills/p2e-mode/SKILL.md` — operating mode (entry point)
- `skills/p2e-mode/references/p2e-model.md` — entity and assessment facts
- `README.md` — installation and MCP configuration
- Product repo docs: `docs/P2E-lifecycle.md`, `docs/P2E-handover.md`
